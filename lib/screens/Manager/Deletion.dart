import 'package:flutter/material.dart';

class DeletePass extends StatefulWidget {
  const DeletePass({Key key}) : super(key: key);

  @override
  _DeletePassState createState() => _DeletePassState();
}

class _DeletePassState extends State<DeletePass> {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.teal.shade700,
    );
  }
}
